---
allowed-tools: Bash(git push:*), Bash(git branch:*)
description: 現在のブランチをリモートにプッシュ
---

# git push

## コンテキスト

- 現在のブランチ: !`git branch --show-current`

## 規約

Git ワークフローの規約は @~/.claude/CLAUDE.md を参照してください。

## タスク

1. `git push -u origin <現在のブランチ>` でリモートにプッシュ
2. プッシュ結果を報告
