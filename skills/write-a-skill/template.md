<!--
  スキル雛形（SKILL.md のひな型）
  この内容を新しいスキルの SKILL.md として起草する。<...> の箇所を埋め、
  HTML コメント（この説明文）は最終成果物から削除すること。

  書き方の要点:
  - description は「何をする」＋「いつ使う（when use xxx）」を必ず含め、要点とトリガー語を前方に置く。
    description と when_to_use の合算は 1,536 文字で切り捨てられるため、長文にしない。
  - workflow は 1. 2. 3. … の番号付きで、各ステップを簡潔な一文にする。
  - 任意フィールド（allowed-tools / model 等）は不要なら行ごと削除する。空欄で残さない。
  - SKILL.md 全体は 100 行以内に収める。長いテンプレや具体例は REFERENCE.md / examples.md へ逃がす。
-->
---
name: <skill-name>
# kebab-case。スキルのディレクトリ名と一致させること。
description: <このスキルが何をするかを一文で>。Use when <いつ使うか＝when use xxx>。トリガー語は「<起動語1>」「<起動語2>」「/<skill-name>」。
# ↑ 要点とトリガー語を前方へ。1,536 文字で切られる点に注意。
# ↑ 値の中で半角コロン＋空白「: 」は使わない（YAML が壊れる）。読点や「＝」で繋ぐこと。
allowed-tools: <Bash(...:*) / Read / Write など、許可なしで使わせたいツールのみ>
# ↑ 任意。スペース or カンマ区切り。不要なら行を削除。
model: <haiku など>
# ↑ 任意。省略するとセッションのモデルを継承。判断を要するスキルは省略推奨。
---

## Context
<!-- 起動時に先回りで集めたい情報があれば、bash 注入で取得する。不要なら本セクションごと削除。 -->
- 収集情報: !`bash ${CLAUDE_SKILL_DIR}/scripts/<収集スクリプト>.sh`

## Additional resources
<!-- 補助ファイルや共通ルールを参照する場合のみ残す。 -->
- 詳細テンプレートは [REFERENCE.md](REFERENCE.md) を参照
- 共通ルールは [<ガイドライン名>](@~/.claude/rules/<file>.md) を参照

## Task（workflow）
<!-- このスキルがやることを番号付きの手順で簡潔に。各ステップは一文。 -->
1. <最初に確認・収集すること>
2. <主たる処理ステップ>
3. <ユーザー確認・承認を挟む場合はそのステップ>
4. <実行・反映>
5. <完了報告・次アクションの案内>

## 規約
<!-- このスキルが必ず守る制約。機密情報の非混入や、承認前に副作用を起こさない等。 -->
- 機密情報（`.env` の値・API キー・トークン）を出力やファイルに含めない
- <破壊的・外向き操作の前にユーザー承認を得る等>

## User Input
$ARGUMENTS
